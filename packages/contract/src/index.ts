/* The /api/* contract: the response shapes the front end is built against
   (AD-11) and the aggregations that produce them, shared by the API and the
   mock API. */
export * from "./types.ts";
export * from "./aggregate.ts";
